apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: nimesh-ns
  name: test-role
rules:
- apiGroups: [""]
  resources: ["services"]
  verbs: ["get", "watch", "list","create","delete"]